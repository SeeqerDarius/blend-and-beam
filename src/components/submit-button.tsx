"use client";import {useFormStatus} from "react-dom";export function SubmitButton({children}:{children:React.ReactNode}){const {pending}=useFormStatus();return <button className="button button-dark submit-button" disabled={pending} aria-disabled={pending} aria-busy={pending} type="submit"><span style={{visibility:pending?"hidden":"visible"}}>{children}</span>{pending&&<span className="submit-progress" role="status">Saving…</span>}</button>}
