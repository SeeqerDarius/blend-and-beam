"use client";
import {useState} from "react";
export function InventorySelector({products,variants}:{products:Array<{id:string;name:string;sku:string}>;variants:Array<{id:string;name:string;product_id:string}>}){const [search,setSearch]=useState("");const [product,setProduct]=useState("");return <><label>Find product by name or SKU<input type="search" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search products…"/></label><label>Product<select name="product" required value={product} onChange={e=>setProduct(e.target.value)}><option value="">Choose product…</option>{products.filter(p=>p.id===product||`${p.name} ${p.sku}`.toLowerCase().includes(search.toLowerCase())).map(p=><option key={p.id} value={p.id}>{p.name} · {p.sku}</option>)}</select></label><label>Variant<select key={product} name="variant" defaultValue=""><option value="">Base product (no variant)</option>{variants.filter(v=>v.product_id===product).map(v=><option value={v.id} key={v.id}>{v.name}</option>)}</select></label></>}
