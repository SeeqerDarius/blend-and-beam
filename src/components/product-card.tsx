import Image from"next/image";import Link from"next/link";import{Heart}from"lucide-react";import{money,type Product}from"@/lib/catalog";
export function ProductCard({product}:{product:Product}){return <article className="product-card"><Link href={`/products/${product.slug}`} className={`product-image ${product.tone}`}>{product.imageUrl?<Image src={product.imageUrl} alt={product.imageAlt??product.name} fill sizes="(max-width: 520px) 50vw, 25vw" style={{objectFit:"contain"}}/>:<span className="product-object">B&amp;B</span>}{product.badge&&<b>{product.badge}</b>}</Link><button aria-label={`Save ${product.name}`}><Heart size={18}/></button><p>{product.category}</p><Link href={`/products/${product.slug}`}><h3>{product.name}</h3></Link><div><strong>{money(product.price)}</strong>{(product.compareAt!==undefined&&product.compareAt>0)&&<del>{money(product.compareAt)}</del>}</div></article>}
