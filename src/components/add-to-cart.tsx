"use client";import{useState}from"react";import type{Product}from"@/lib/catalog";export function AddToCart({product}:{product:Product}){const[added,setAdded]=useState(false);return <button className="add-button" onClick={()=>{const items=JSON.parse(localStorage.getItem("bb-cart")??"[]")as Array<{slug:string;quantity:number}>;const found=items.find(i=>i.slug===product.slug);if(found)found.quantity+=1;else items.push({slug:product.slug,quantity:1});localStorage.setItem("bb-cart",JSON.stringify(items));setAdded(true)}}>{added?"Added — view cart":"Add to cart"}</button>}
