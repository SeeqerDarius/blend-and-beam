import type{MetadataRoute}from"next";import{getCatalog}from"@/lib/catalog-data";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const b=process.env.NEXT_PUBLIC_APP_URL??"http://localhost:3000";const{products}=await getCatalog();return["","/shop","/about","/contact","/faq"].map(p=>({url:b+p,lastModified:new Date()})).concat(products.map(p=>({url:`${b}/products/${p.slug}`,lastModified:new Date()})))}
