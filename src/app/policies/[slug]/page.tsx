import{notFound}from"next/navigation";import{InfoPage}from"@/components/info-page";const content={returns:["Returns & refunds","Return windows, eligible conditions, restocking rules, damaged-delivery evidence, and refund timelines require owner and legal approval before orders open."],privacy:["Privacy policy","The final policy must identify the operating legal entity, lawful processing purposes, retention periods, customer rights, processors, and contact details."],terms:["Terms & conditions","Commercial terms, governing law, warranty limits, delivery acceptance, pricing corrections, and dispute handling require owner and legal approval before launch."]};export function generateStaticParams(){return Object.keys(content).map(slug=>({slug}))}export default async function Policy({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=content[slug as keyof typeof content];if(!item)notFound();return <InfoPage eyebrow="OWNER / LEGAL REVIEW REQUIRED" title={item[0]}><p>{item[1]}</p><p>This page is deliberately not presented as final legal advice.</p></InfoPage>}
