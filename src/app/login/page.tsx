import{StoreHeader}from"@/components/store-header";import{signIn,signUp}from"./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const q=await searchParams;return <><StoreHeader/><main className="auth-page"><section><p className="eyebrow">WELCOME</p><h1>Your account</h1>{q.error&&<p className="form-error">{q.error}</p>}{q.message&&<p className="form-success">{q.message}</p>}<form className="stack-form"><label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" minLength={8} autoComplete="current-password" required/></label><button formAction={signIn} className="button button-dark">Sign in</button><button formAction={signUp} className="button secondary">Create account</button></form></section></main></>}
