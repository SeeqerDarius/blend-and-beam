import Link from "next/link";import {staffPermissions} from "@/lib/admin-auth";import {adminLinks} from "@/lib/admin-resources";
export default async function Admin(){const permissions=await staffPermissions();const available=adminLinks.slice(1).map(l=>({l,yes:permissions.has(l[2])}));return <><p className="eyebrow">BLEND &amp; BEAM</p><h1>Store operations</h1><p>Manage your catalog, stock and customer orders. Every section is permission-controlled.</p><div className="operations-notice">Payment on delivery is available in enabled delivery zones. Online payments require Paystack configuration and verification.</div><div className="operations-grid">{available.filter(x=>x.yes).map(({l})=><Link className="operations-card" key={l[1]} href={l[1]}><h2>{l[0]}</h2><span>Open workspace →</span></Link>)}</div></>}
