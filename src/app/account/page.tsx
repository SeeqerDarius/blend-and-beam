import {money} from "@/lib/catalog";import Link from"next/link";import{redirect}from"next/navigation";import{StoreHeader}from"@/components/store-header";import{createClient}from"@/lib/supabase/server";
export default async function Account(){const supabase=await createClient();const{data}=await supabase.auth.getUser();if(!data.user)redirect("/login");const{data:orders}=await supabase.from("orders").select("order_number,status,payment_method,payment_status,total_minor,created_at").eq("user_id",data.user.id).order("created_at",{ascending:false}).limit(10);return <><StoreHeader/><main className="page-intro"><p className="eyebrow">MY ACCOUNT</p><h1>Hello.</h1><p>{data.user.email}</p><p><Link href="/account/security">Account security</Link> · <Link href="/admin">Staff workspace</Link></p><h2>Recent orders</h2>{orders?.length?<ul>{orders.map(o=><li key={o.order_number}>{o.order_number} — {o.status} · {o.payment_method==="cod"?"Payment on delivery":"Online payment"} · {o.payment_status} · {money(o.total_minor)}</li>)}</ul>:<div className="empty-state"><strong>No orders yet</strong><p>Your purchases and delivery progress will appear here.</p></div>}</main></>}
