export function slugify(name:string){return name.normalize("NFKD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,150)}
export function draftCopy(name:string,category:string,details:string){const title=name.trim();return {slug:slugify(title),description:[title,category?`Category: ${category}.`:"",details.trim()].filter(Boolean).join("\n\n"),seo_title:`${title} | Blend & Beam`.slice(0,60),seo_description:`Explore ${title}${category?` in ${category}`:""} at Blend & Beam. Contact our team for availability and delivery in Ghana.`.slice(0,160)}}
export function toMinor(value:string){if(!/^\d+(\.\d{1,2})?$/.test(value))throw new Error("Enter a non-negative GHS amount with at most two decimal places.");const [whole,fraction=""]=value.split(".");const amount=Number(whole)*100+Number(fraction.padEnd(2,"0"));if(!Number.isSafeInteger(amount)||amount>100000000)throw new Error("Amount is too large.");return amount}
